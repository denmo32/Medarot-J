/**
 * @file BattleSequenceContext.js
 * @description アクションシーケンスの実行状態を保持するコンポーネント。
 */
export class BattleSequenceContext {
    constructor() {
        this.isSequenceRunning = false; // シーケンス実行中かどうか
    }
}